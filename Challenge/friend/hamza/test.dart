import 'dart:io';
import 'dart:io';

void main() {
  print('Enter a word : ');
  String? word = stdin.readLineSync();
  String n = word.toString();
  
  // ---------------- word to list ---------------
  List to_List = [];
  for (var i in n){
    t.add(i) ;
  }
  // ---------------- test letter ---------------
  List t = [];
  for (int i = 0; i == n; i++) {
    if ((word[i] == 'a') || (word[i] == 'i') || (word[i] == 'e') || (word[i] == 'i') || (word[i] == 'o') ||(word[i] == 'u') || (word[i] == 'y'))
  }


 


}
